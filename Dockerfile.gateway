# CyxCloud Gateway Dockerfile (For Railway deployment)
# Pulls pre-built image from GitHub Container Registry

FROM ghcr.io/cyxwiz-lab/cyxcloud/gateway:latest

# Environment can be overridden by Railway
ENV RUST_LOG=info
ENV GATEWAY_HOST=0.0.0.0
ENV GATEWAY_HTTP_PORT=8080
ENV GATEWAY_GRPC_PORT=50052
